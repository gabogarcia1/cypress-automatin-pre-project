/// <>

describe("empty spec", () => {
  it("passes", () => {
    cy.visit("https://pushing-front.vercel.app/");
    //css selector es una forma de localizar los elementos de la pagina web utilizando cypress
    //en el arbol de inspect hacemos control f, si ponems buttons nos dice la cantidad de buttons que existe dentro del html. si ponemos input
    //
    //Pasos para instalar cypress
    //     npm init -y
    // npm install cypress@10.3.0 --save-dev
  });
});
